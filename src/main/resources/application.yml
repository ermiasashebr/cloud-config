server:
  port: 8888

spring:
  application:
    name: cloudConfig
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ermiasashebr/cloud-config
          clone-on-start: true
          default-label: main
          

